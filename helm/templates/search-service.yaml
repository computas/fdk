apiVersion: v1
kind: Service
metadata:
  creationTimestamp: null
  labels:
    fdk.service: search
  name: search
  namespace: {{ required "Missing value NAMESPACE" .Values.NAMESPACE }}
spec:
  type: NodePort
  ports:
  - name: "3000"
    port: 3000
    targetPort: 3000
  selector:
    fdk.service: search
status:
  loadBalancer: {}
