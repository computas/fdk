properties([pipelineTriggers([githubPush()])])

node {
    stage ('Checkout'){
        echo "check out"
    }
    stage ('Build'){
      echo "build out"
        // steps
    }
    stage ('Test'){
      echo "test"
    }
}
